package com.ddl.unirides.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta principal - Verde oscuro universitario
val PrimaryGreen = Color(0xFF1A2F26)       // Fondo principal oscuro
val PrimaryGreenLight = Color(0xFF2D4A3E)  // Surface/Cards
val PrimaryGreenDark = Color(0xFF0D1713)   // Más oscuro para contraste

// Paleta secundaria - Turquesa/Cyan para acciones y acentos
val AccentCyan = Color(0xFF00BFA5)         // Botones, links, iconos activos
val AccentCyanLight = Color(0xFF5DF2D6)    // Hover/Pressed states
val AccentCyanDark = Color(0xFF008E76)     // Variante oscura

// Colores adicionales
val ErrorRed = Color(0xFFEF5350)           // Errores y logout
val WarningOrange = Color(0xFFFF9800)      // Advertencias
val SuccessGreen = Color(0xFF66BB6A)       // Éxitos

// Colores de superficie y fondo
val BackgroundDark = Color(0xFF1A2F26)     // Fondo principal
val SurfaceDark = Color(0xFF2D4A3E)        // Cards, elevated surfaces
val SurfaceVariant = Color(0xFF3D5A4D)     // Variante de surface

// Colores de texto
val TextPrimary = Color(0xFFFFFFFF)        // Texto principal blanco
val TextSecondary = Color(0xFFB0BEC5)      // Texto secundario gris claro
val TextDisabled = Color(0xFF78909C)       // Texto deshabilitado
val TextOnAccent = Color(0xFF1A2F26)       // Texto sobre botones cyan (oscuro)

// Colores de división
val DividerColor = Color(0xFF3D5A4D)
val OutlineColor = Color(0xFF546E7A)

// Color para badges y estados
val BadgeBackground = Color(0xFF2D4A3E)
val BadgeText = Color(0xFF00BFA5)
