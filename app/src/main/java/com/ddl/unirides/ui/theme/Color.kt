package com.ddl.unirides.ui.theme

import androidx.compose.ui.graphics.Color

// ===== TEMA CLARO =====
// Azul primario moderno
val BluePrimary = Color(0xFF4285F4)           // Azul brillante principal
val BluePrimaryLight = Color(0xFF669DF6)      // Azul claro para hover
val BluePrimaryDark = Color(0xFF1967D2)       // Azul oscuro para pressed

// Fondos y superficies - Tema Claro
val BackgroundLight = Color(0xFFFAFAFA)       // Fondo gris muy claro
val SurfaceLight = Color(0xFFFFFFFF)          // Blanco puro para cards
val SurfaceVariantLight = Color(0xFFF5F5F5)   // Gris claro para variantes

// Texto - Tema Claro
val TextPrimaryLight = Color(0xFF1A1A1A)      // Negro/gris muy oscuro
val TextSecondaryLight = Color(0xFF5F6368)    // Gris medio
val TextDisabledLight = Color(0xFF9AA0A6)     // Gris claro

// ===== TEMA OSCURO =====
// Fondos y superficies - Tema Oscuro
val BackgroundDark = Color(0xFF121212)        // Negro/gris muy oscuro
val SurfaceDark = Color(0xFF1E1E1E)           // Gris oscuro para cards
val SurfaceVariantDark = Color(0xFF2C2C2C)    // Gris oscuro variante

// Texto - Tema Oscuro
val TextPrimaryDark = Color(0xFFFFFFFF)       // Blanco puro
val TextSecondaryDark = Color(0xFFB0B0B0)     // Gris claro
val TextDisabledDark = Color(0xFF707070)      // Gris medio

// ===== COLORES COMPARTIDOS =====
// Colores de estado
val ErrorRed = Color(0xFFEF5350)              // Rojo para errores
val WarningOrange = Color(0xFFFF9800)         // Naranja para advertencias
val SuccessGreen = Color(0xFF4CAF50)          // Verde para éxito

// Colores secundarios/acentos
val BlueAccent = Color(0xFF03A9F4)            // Azul claro para acentos secundarios
val BlueLight = Color(0xFFE3F2FD)             // Azul muy claro para badges (tema claro)
val BlueLightDark = Color(0xFF1E3A5F)         // Azul oscuro para badges (tema oscuro)

// Bordes y divisores
val DividerLight = Color(0xFFE0E0E0)          // Divisor tema claro
val DividerDark = Color(0xFF3C3C3C)           // Divisor tema oscuro
val OutlineLight = Color(0xFFBDBDBD)          // Outline tema claro
val OutlineDark = Color(0xFF5F5F5F)           // Outline tema oscuro

// Texto sobre azul primario
val TextOnPrimary = Color(0xFFFFFFFF)         // Blanco sobre azul
